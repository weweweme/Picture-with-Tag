#include "DataManagementPage.h"

DataManagementPage::DataManagementPage(const wxString& title, const wxPoint& pos, const wxSize& size, const PageID currentPage)
        : BasePage(title, pos, size, currentPage) {
    DataManagementPage::SetupPage();
}

void DataManagementPage::SetupPage() {

}
